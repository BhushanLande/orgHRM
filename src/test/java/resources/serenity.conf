browser = ${?browser}
browser = ${?BROWSER}

webdriver.driver=chrome
webdriver.base.url="https://opensource-demo.orangehrmlive.com/web/index.php/auth/login"

webdriver {
  driver = provided

  provided {
    type = custom
    custom = "resources.DriverProvider.HealeniumDriverProvider"
  }
}
